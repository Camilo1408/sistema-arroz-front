# .env
# URLs del backend (desarrollo)
VITE_API_URL=http://localhost:8000/api/v1
VITE_WS_URL=ws://localhost:8000/ws

# .env — AGREGA ESTAS LÍNEAS al archivo existente

# ── Base de datos ──────────────────────────────────────────────
# Desarrollo: SQLite local (no necesita instalación)
DATABASE_URL=sqlite+aiosqlite:///./diagnostico_arroz.db

# Producción (Supabase): descomentar y pegar la URL real
# DATABASE_URL=postgresql+asyncpg://usuario:password@host:5432/postgres